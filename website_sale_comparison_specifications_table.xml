<t name="Specifications Table" t-name="website_sale_comparison.specifications_table">
        <table t-attf-class="table {{is_accordion and 'table-sm mb-0'}}">
            <t t-if="len(attrib_categories) &gt; 1 and not is_accordion">
                <tr>
                    <th class="text-start" colspan="2">
                        <span t-if="category" t-field="category.name"/>
                        <span t-else="">Otros</span>
                    </th>
                </tr>
            </t>
            <tr t-foreach="attrib_categories[category].filtered(lambda l: len(l.value_ids) &gt; 1)" t-as="ptal">
                <t t-set="hide_border_bottom_classes" t-value="'border-bottom-0' if ptal_last and is_accordion else ''"/>
                <td t-attf-class="w-25 {{hide_border_bottom_classes}} ps-0">
                    <span t-field="ptal.attribute_id.name"/>
                </td>
                <td t-attf-class="w-75 {{hide_border_bottom_classes}} pe-0 text-muted text-end">
                    <t t-foreach="ptal.value_ids" t-as="pav">
                        <span t-field="pav.name"/><t t-if="not pav_last">, </t>
                    </t>
                </td>
            </tr>
            <t t-set="single_value_attributes" t-value="attrib_categories[category]._prepare_single_value_for_display()"/>
            <tr t-foreach="single_value_attributes" t-as="attribute">
                <t t-set="hide_border_bottom_classes" t-value="'border-bottom-0' if attribute_last and is_accordion else ''"/>
                <td t-attf-class="w-25 {{hide_border_bottom_classes}} ps-0 ">
                    <span t-field="attribute.name"/>
                </td>
                <td t-attf-class="w-75 {{hide_border_bottom_classes}} pe-0 text-muted text-end">
                    <t t-foreach="single_value_attributes[attribute]" t-as="ptal">
                        <span t-field="ptal.product_template_value_ids._only_active().name"/><t t-if="not ptal_last">, </t>
                    </t>
                </td>
            </tr>
        </table>
    </t>
