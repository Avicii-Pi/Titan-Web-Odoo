<data inherit_id="website.layout" priority="1">
        <body position="before">
            <t t-if="website._get_product_image_ratio() != '1_1'" t-set="body_classname" t-value="'o_wsale_products_opt_thumb_' + website._get_product_image_ratio() + (' ' + body_classname if body_classname else '')"/>
        </body>
        <head position="inside">
            <!-- Company markup data, in all pages. -->
            <t t-set="base_url" t-value="website.get_base_url()"/>
            <script t-if="res_company and website" type="application/ld+json">
{
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": "<t t-out="res_company.name"/>",
    "logo": "<t t-out="'%s/logo.png?company=%s' % (base_url, res_company.id)"/>",
    "url": "<t t-out="base_url"/>"
}
            </script>

             <!-- Product markup data, on product pages. -->
            <script t-if="product_markup_data" type="application/ld+json" t-out="product_markup_data"/>
        </head>
    </data>
