<t name="Header Wishlist Link" t-name="website_sale_wishlist.header_wishlist_link">
        <t t-set="wishcount" t-value="len(request.env['product.wishlist'].current())"/>
        <t t-set="show_wishes" t-value="website.has_ecommerce_access()"/>
        <li t-attf-class="o_wsale_my_wish #{not show_wishes and 'd-none'} #{_item_class}">
            <a href="/shop/wishlist" t-attf-class="#{_link_class}">
                <div t-attf-class="#{_icon_wrap_class}">
                    <i t-if="_icon" class="fa fa-1x fa-heart fa-stack"/>
                    <sup t-out="wishcount" t-attf-class="my_wish_quantity o_animate_blink badge bg-primary #{_badge_class} #{'d-none' if (wishcount == 0) else ''}"/>
                </div>
                <span t-if="_text" t-attf-class="#{_text_class}" class="small">Lista Deseos</span>
            </a>
        </li>
    </t>
